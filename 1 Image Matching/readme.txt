相对配准/绝对配准 主要的计算过程是一样的。

差别在于相对配准不需要地面点实际坐标，只需要参考影像上的坐标(x, y)。而绝对配准则将参考影像的坐标(x, y) 替换为了实际地点的坐标 (X, Y).
注意，参考影像上的坐标和实际地点的坐标在P130里面都是以（X， Y）的形式出现的。


参考影像 待纠正影像 。 确定了从待纠正的影像 到 参考影像的转换系数，就可以求出待纠正影像转换过去的大致区域。

间接法 ： 从空白图像出发，反算在原始图像上的坐标，再二次插值得出结果